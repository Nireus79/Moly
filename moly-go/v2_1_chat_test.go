package main

import "testing"

// ChatServer tests are disabled - ChatServer type doesn't exist
// Use V2APIServer tests instead
func TestChatServerDisabled(t *testing.T) {
	t.Skip("ChatServer tests disabled - use V2APIServer tests")
}
